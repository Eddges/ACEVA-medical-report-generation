import React from 'react'
import classes from './ReportField.module.css'

const ReportField = (props) => {
    return (
        <div className={classes.Container}>
            <span className={classes.Title}>Vitamin B12</span>
        </div>
    )
}

export default ReportField